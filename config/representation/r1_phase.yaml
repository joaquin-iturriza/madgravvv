# @package representation
# R1: add the phase channel. Costs 16*1*3*3 = 144 parameters in conv1 (1->32 becomes
# 2->32 gives 288), which is inside the C2 tolerance but must still be reported.
defaults:
  - baseline
  - _self_
phase_channel: true
